# Pinned to the versions this project was developed and evaluated with, so the
# paper's numbers reproduce. Bump deliberately, not incidentally.
torch==2.13.0
torchvision==0.28.0
numpy==2.5.2
scipy==1.18.0
scikit-learn==1.9.0
opencv-python==5.0.0.93
Pillow==12.3.0
tqdm==4.70.0
PyYAML==6.0.3

# backend (inference server)
fastapi==0.141.1
uvicorn[standard]==0.52.1
python-multipart==0.0.32
